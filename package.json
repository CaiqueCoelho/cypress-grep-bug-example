{
  "name": "cypress-grep-bug-example",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "inverted:tag": "npx cypress run --browser chrome --env grepTags=-@burn",
    "grepUntagged": "npx cypress run --browser chrome --env grepUntagged=true"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@cypress/grep": "^4.0.1",
    "cypress": "^13.4.0"
  }
}
